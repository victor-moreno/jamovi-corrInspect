.Rhistory
.RData
.Rproj.user
build/
*.jmo
.tmp/
Rplots.pdf

# Generated by jmvtools::prepare()/install(), which the build tooling
# (tools/install.sh) always runs fresh -- not pre-committing it avoids the
# stale-file churn of tracking a file this sandbox can't regenerate itself
# (see task_plan.md / jamovi_build_toolchain memory).
R/corrinspect.h.R
R/00jmv.R
# jmvtools compiles jamovi/i18n/*.po into this on every build -- same
# stale-file reasoning as above; jamovi/i18n/*.po is the real source.
inst/i18n/
